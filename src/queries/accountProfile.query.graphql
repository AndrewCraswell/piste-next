query AccountProfile($oid: String!) {
  Accounts(where: { Oid: { _eq: $oid } }) {
    Oid
    PrimaryStudentId
    AccountStudent {
      StudentId
      FirstName
      LastName
      Email
      Phone
      Birthdate
      AssociationMemberId
    }
    Address {
      AddressId
      Address
      Address2
      City
      Postal
      State
    }
    calendar {
      id
      account_id
      access_token
      created_at
    }
  }
}
