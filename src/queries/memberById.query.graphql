query MemberById($id: String!) @cached(ttl: 300) {
  member: AssociationMembers(where: { AssociationMemberId: { _eq: $id } }) {
    FirstName
    LastName
    AssociationMemberId
    Club1Name
    Division
    Birthdate
  }
}
