query SearchMembers($filter: String!) {
  MembersLookup(
    limit: 25
    where: { FullName: { _like: $filter } }
    order_by: { FullName: asc }
  ) {
    FullName
    MemberId
    Member {
      Club1Name
      Club2Name
    }
  }
}
