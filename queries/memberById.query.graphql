query MemberById($id: String!) {
  member: AssociationMembers(where: { AssociationMemberId: { _eq: $id } }) {
    FirstName
    LastName
    AssociationMemberId
    Club1Name
    Division
    Birthdate
  }
}
